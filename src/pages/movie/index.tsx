import * as React from 'react';
import MovieListContainer from '../../container/MoviceListContainer';

const MemoPage: React.FC = () => {
  return (
    <div className="container">
        <MovieListContainer></MovieListContainer>
    </div>
  )
}

export default MemoPage;